<div class="container">
  <h3>Add a team role to <%= @team.name %></h3>
<% if @team.nil? %>
  <%= render 'team_form' %>
<% else %>
  <%= render 'team_role_form' %>
<% end %>
  <hr>
  <%= link_to "Back to the team", team_path(@team), class: "btn btn-secondary" %>
</div>
