<%= simple_form_for [@team, @team_role] do |f| %>
  <%= f.input :name %>
  <%= f.association :user, label_method: :network_id, value_method: :id%>
  <%= f.input :occupancy, input_html: { min: 1, max: 100 }  %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
